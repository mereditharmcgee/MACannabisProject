---
phase: 05-search-system
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03

must_haves:
  truths:
    - "User confirms search bar is visible and prominent in hero section"
    - "User confirms typing a dispensary name filters the card grid instantly"
    - "User confirms town search shows correct dispensaries"
    - "User confirms owner search shows correct dispensaries"
    - "User confirms clearing search restores all cards"
    - "User confirms URL updates with ?q= parameter"
  artifacts: []
  key_links: []
---

<objective>
Verify the search system works correctly through manual browser testing.

Purpose: Client-side search behavior (instant filtering, fuzzy matching, URL state, back button) cannot be fully validated by unit tests alone. This checkpoint confirms the end-to-end experience.

Output: User approval of search functionality.
</objective>

<execution_context>
@C:/Users/mmcge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mmcge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-search-system/05-CONTEXT.md
@.planning/phases/05-search-system/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and functional verification of search system</name>
  <files>N/A</files>
  <action>
    Run `npm run dev` and present the site for user verification. The search system built in Plan 01 needs manual browser testing to confirm the end-to-end experience.

    What was built: Complete search system with search bar in hero section, instant typeahead filtering across dispensary names/towns/owners, pre-built Fuse.js index as static JSON, URL state sync with ?q= parameter, zero-results messaging, and clear button.

    Steps for user to verify:
    1. Search bar appearance: Large search bar visible in hero section with magnifying glass icon and placeholder "Search by name, town, or owner"
    2. Name search: Type a dispensary name (e.g., "NETA") -- card grid filters to show only matching dispensaries
    3. Town search: Type a town name (e.g., "Worcester") -- all dispensaries in that town appear
    4. Owner search: Type an owner name (e.g., "Curaleaf") -- all dispensaries by that owner appear
    5. Fuzzy matching: Type a slight misspelling (e.g., "Worcster") -- should still match Worcester dispensaries
    6. Result count: The "Showing X of 525 dispensaries" count updates as you type
    7. Clear button: An X button appears when text is entered; clicking it restores all 525 cards
    8. Zero results: Type something that matches nothing (e.g., "zzzzzzz") -- shows "No dispensaries match 'zzzzzzz'" message
    9. URL state: After searching, check URL contains ?q=yourquery; copy URL, open in new tab, search is pre-filled
    10. Back button: Search something, then clear, then press back -- restores the previous search
    11. Mobile: Resize browser to mobile width -- search bar is full-width and usable
    12. Relevance ordering: Search for a term that matches both names and towns -- name matches appear first

    Resume signal: Type "approved" or describe any issues.
  </action>
  <verify>User types "approved" or provides feedback for iteration</verify>
  <done>User confirms search system meets all acceptance criteria from CONTEXT.md locked decisions</done>
</task>

</tasks>

<verification>
User confirms search system meets all acceptance criteria from CONTEXT.md locked decisions.
</verification>

<success_criteria>
- User approves search bar appearance and placement
- User confirms instant filtering works for name, town, and owner queries
- User confirms URL state, back button, and clear button work correctly
- User confirms zero-results message displays properly
</success_criteria>

<output>
After completion, create `.planning/phases/05-search-system/05-02-SUMMARY.md`
</output>
