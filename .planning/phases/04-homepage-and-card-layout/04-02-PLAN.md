---
phase: 04-homepage-and-card-layout
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/pages/index.astro
autonomous: false
requirements: [DSGN-01, DSGN-03]

must_haves:
  truths:
    - "Homepage displays 'Who Owns Your Dispensary?' headline"
    - "Homepage shows stats: total licenses from stats.json, 92% independently owned (hardcoded), total towns from stats.json"
    - "Homepage displays all 525 dispensaries as cards in a responsive grid"
    - "Grid shows 1 column on mobile, 2 on tablet, 3 on desktop"
    - "Clicking any card navigates to /dispensary/[slug]/"
    - "Visible count reads 'Showing 525 dispensaries' above the grid"
    - "Footer shows 'A project by Meredith McGee' with brief data source line"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Complete homepage with hero, stats, card grid, footer"
      contains: "Who Owns Your Dispensary"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/DispensaryGridCard.astro"
      via: "import and map rendering"
      pattern: "import DispensaryGridCard"
    - from: "src/pages/index.astro"
      to: "astro:content"
      via: "getCollection for dispensaries and stats"
      pattern: "getCollection.*dispensaries"
---

<objective>
Replace the placeholder homepage with the full production page: hero section with headline, subtitle, dynamic stats, a responsive card grid of all 525 dispensaries, and an expanded footer.

Purpose: DSGN-01 (responsive layout) and DSGN-03 (prominent headline with key stats) are the core homepage requirements. This plan wires the DispensaryGridCard from Plan 01 into the full page.
Output: A complete, responsive homepage at / showing all 525 dispensaries.
</objective>

<execution_context>
@C:/Users/mmcge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mmcge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-homepage-and-card-layout/04-CONTEXT.md
@.planning/phases/04-homepage-and-card-layout/04-RESEARCH.md
@.planning/phases/04-homepage-and-card-layout/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/components/DispensaryGridCard.astro:
```astro
interface Props {
  dispensary: {
    tradeName: string;
    slug: string;
    town: string | null;
    owner: string | null;
    licenseType: string | null;
    specialStatusTags: string[];
  };
}
```

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title?: string;
  description?: string;
}
<!-- Wraps content in html/head/body with Tailwind, bg-gray-50 body -->
```

From src/content.config.ts:
```typescript
// getCollection('dispensaries') returns entries with .data containing schema fields
// getCollection('stats') returns entries with .data: { totalLicenses, percentIndependent, totalTowns }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete homepage</name>
  <files>src/pages/index.astro</files>
  <action>
    Replace src/pages/index.astro entirely with the production homepage. Structure:

    **Frontmatter:**
    - Import BaseLayout, DispensaryGridCard, getCollection
    - Fetch dispensaries via getCollection('dispensaries'), map to .data, sort alphabetically by tradeName (localeCompare)
    - Fetch stats via getCollection('stats'), extract totalLicenses and totalTowns from first entry (fallback: 525, 156)

    **Hero section** (not sticky, scrolls away):
    - `<section>` with text-center, py-12 px-4, bg-gray-50 (matches body)
    - h1: "Who Owns Your Dispensary?" -- text-4xl font-bold tracking-tight text-gray-900 sm:text-5xl
    - Subtitle paragraph: "A transparent directory of every licensed cannabis dispensary in Massachusetts and who owns them." -- mt-3 text-lg text-gray-600 max-w-2xl mx-auto
    - Comment placeholder: `<!-- Phase 5: search bar goes here -->`
    - Stats bar (mt-8): grid grid-cols-1 sm:grid-cols-3 gap-4 max-w-lg mx-auto
      - Box 1: totalLicenses (from stats.json) / "Active Licenses" -- green-700 text, white bg, rounded-lg shadow-sm
      - Box 2: "92%" (hardcoded per user decision) / "Independently Owned"
      - Box 3: totalTowns (from stats.json) / "Towns"

    **Card grid section:**
    - Container: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8
    - Count line above grid: `<p class="text-sm text-gray-500 mb-4">Showing {sorted.length} dispensaries</p>`
    - Comment placeholder: `<!-- Phase 6: filter pills go here -->`
    - Grid: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6
    - Map sorted dispensaries to DispensaryGridCard components

    **Footer:**
    - `<footer>` with max-w-7xl mx-auto px-4 py-8 border-t border-gray-200 text-center
    - Line 1: "A project by Meredith McGee" -- text-sm text-gray-500
    - Line 2: "Data sourced from Massachusetts Cannabis Control Commission public records." -- text-xs text-gray-400 mt-1
    - Comment placeholder: `<!-- Phase 7: Terms of Service link goes here -->`

    Wrap everything in BaseLayout (no custom title/description needed -- defaults are good).

    IMPORTANT: Do NOT add a search bar or filter pills. Only leave HTML comments marking where they will go in Phase 5/6.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -5 && grep -c "dispensary/" dist/index.html | head -1</automated>
  </verify>
  <done>Homepage builds successfully. dist/index.html contains "Who Owns Your Dispensary?" headline, 3 stats boxes with dynamic values, 525 card links to /dispensary/[slug]/, "Showing 525 dispensaries" count, and expanded footer. Grid uses responsive classes (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of homepage</name>
  <action>
    User visually verifies the complete homepage at all three responsive breakpoints.
  </action>
  <what-built>Complete homepage with hero section (headline, subtitle, stats), responsive card grid of all 525 dispensaries, and footer. Cards show dispensary name (title-cased), town, owner, license type label, and ownership badges.</what-built>
  <how-to-verify>
    1. Run `npx astro dev` and visit http://localhost:4321/
    2. Verify hero: "Who Owns Your Dispensary?" headline, subtitle, 3 stats boxes (Active Licenses, Independently Owned, Towns)
    3. Scroll down: "Showing 525 dispensaries" label, then card grid
    4. Check cards show: title-cased name, town, owner, small license type label (Storefront/Delivery/etc), colored badges
    5. Resize browser window:
       - ~375px wide: 1 column of cards
       - ~768px wide: 2 columns
       - ~1024px+ wide: 3 columns
    6. Hover a card: subtle shadow lift
    7. Click a card: navigates to /dispensary/[slug]/ detail page
    8. Scroll to bottom: footer with "A project by Meredith McGee" and data source line
    9. Overall: Does it feel like a "polished restaurant guide" as intended?
  </how-to-verify>
  <verify>User confirms visual design matches expectations</verify>
  <done>User approves homepage layout, card design, and responsive behavior</done>
  <resume-signal>Type "approved" or describe any issues with layout, styling, or content</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all existing tests still pass
- `npx astro build` -- full build succeeds, 525+ pages generated
- `grep "Who Owns Your Dispensary" dist/index.html` -- headline present
- `grep -c "dispensary/" dist/index.html` -- 525+ card links present
- Visual inspection at 3 breakpoints confirms responsive grid
</verification>

<success_criteria>
- Homepage replaces placeholder with full hero + card grid + footer
- Stats display dynamically from stats.json (totalLicenses, totalTowns) with 92% hardcoded
- All 525 dispensaries render as cards sorted A-Z
- Responsive grid: 1 col mobile, 2 col tablet, 3 col desktop
- Cards link to /dispensary/[slug]/ with trailing slash
- User approves visual design at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/04-homepage-and-card-layout/04-02-SUMMARY.md`
</output>
