---
phase: 03-detail-pages
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/pages/dispensary/[slug].astro
  - src/components/DispensaryCard.astro
  - src/components/OwnershipBadge.astro
  - src/components/SiblingLinks.astro
  - src/components/JsonLd.astro
  - astro.config.mjs
autonomous: true
requirements: [DETL-01, DETL-02, DETL-03, DETL-05, DETL-06, DETL-07]

must_haves:
  truths:
    - "Navigating to /dispensary/[slug]/ loads a pre-rendered HTML page for each dispensary"
    - "Each page displays dispensary name, owner, address (tappable to Google Maps), phone (tap-to-call), license type"
    - "Ownership badges are visually prominent at top of page"
    - "Ownership narrative paragraph appears on page (or researching notice for pending/inconclusive)"
    - "Sibling location links appear at bottom for multi-location owners"
    - "JSON-LD script tag present in page source with schema.org Store data"
    - "Meta title follows format: Who Owns [Name]? | MA Cannabis Directory"
    - "researchInconclusive pages hide owner, narrative, and sibling sections"
  artifacts:
    - path: "src/pages/dispensary/[slug].astro"
      provides: "Dynamic route generating 525 pages via getStaticPaths"
      contains: "getStaticPaths"
    - path: "src/components/DispensaryCard.astro"
      provides: "Profile card with name, badges, owner, facts, narrative"
      min_lines: 40
    - path: "src/components/OwnershipBadge.astro"
      provides: "Colored badge pill for specialStatusTags"
    - path: "src/components/SiblingLinks.astro"
      provides: "Cross-links to sibling locations"
    - path: "src/components/JsonLd.astro"
      provides: "JSON-LD script tag injection"
  key_links:
    - from: "src/pages/dispensary/[slug].astro"
      to: "src/components/DispensaryCard.astro"
      via: "import and render"
      pattern: "import DispensaryCard"
    - from: "src/pages/dispensary/[slug].astro"
      to: "astro:content"
      via: "getCollection('dispensaries') in getStaticPaths"
      pattern: "getCollection.*dispensaries"
    - from: "src/pages/dispensary/[slug].astro"
      to: "src/lib/narrative.ts"
      via: "import generateNarrative"
      pattern: "import.*generateNarrative"
    - from: "src/pages/dispensary/[slug].astro"
      to: "src/lib/jsonld.ts"
      via: "import buildJsonLd for JsonLd component"
      pattern: "import.*buildJsonLd"
    - from: "src/pages/dispensary/[slug].astro"
      to: "src/lib/siblings.ts"
      via: "import groupByParentCompany for sibling computation"
      pattern: "import.*groupByParentCompany"
    - from: "src/pages/dispensary/[slug].astro"
      to: "src/lib/seo-meta.ts"
      via: "import buildMetaTitle and buildMetaDescription for SEO"
      pattern: "import.*buildMetaTitle"
---

<objective>
Build the Astro dynamic route and components that render 525 dispensary detail pages. Each page displays a clean profile card with dispensary info, ownership badges, narrative, JSON-LD structured data, SEO meta tags, and sibling location cross-links.

Purpose: This is the core deliverable of Phase 3 -- the actual pages that Google will index and users will visit. All lib functions from Plan 01 are consumed here.
Output: Dynamic route file, 4 Astro components, 525 pre-rendered HTML pages on build.
</objective>

<execution_context>
@C:/Users/mmcge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mmcge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-detail-pages/03-CONTEXT.md
@.planning/phases/03-detail-pages/03-RESEARCH.md
@.planning/phases/03-detail-pages/03-01-SUMMARY.md

<interfaces>
<!-- Interfaces created by Plan 01 that this plan consumes -->

From src/lib/slugs.ts (created in Plan 01):
```typescript
export function stripLegalSuffix(name: string): string;
export function generateSlug(tradeName: string): string;
export function deduplicateSlugs(records: Array<{ tradeName: string; town: string | null }>): string[];
```

From src/lib/narrative.ts (created in Plan 01):
```typescript
export function generateNarrative(d: {
  tradeName: string;
  owner: string | null;
  town: string | null;
  ownershipDetails: string | null;
  needsNarrative: boolean;
  researchInconclusive: boolean;
}): { text: string; type: 'full' | 'pending' | 'inconclusive' };
```

From src/lib/jsonld.ts (created in Plan 01):
```typescript
export function buildJsonLd(dispensary: {
  tradeName: string;
  address: string | null;
  town: string | null;
  phone: string | null;
  slug: string;
}, siteUrl: string): object;
```

From src/lib/siblings.ts (created in Plan 01):
```typescript
export function groupByParentCompany(records: Array<{
  parentCompany: string | null;
  slug: string;
  researchInconclusive: boolean;
}>): Map<string, Array<...>>;
export function getSiblings(currentSlug: string, allRecords: Array<...>, siblingMap: Map<...>): Array<...>;
```

From src/lib/seo-meta.ts (created in Plan 01):
```typescript
export function buildMetaTitle(tradeName: string): string;
export function buildMetaDescription(d: {
  tradeName: string;
  town: string | null;
  licenseType: string | null;
}): string;
```

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title?: string;
  description?: string;
}
<!-- Renders <html>, <head> with meta, <body> with <slot /> -->
```

From src/schemas/dispensary.ts:
```typescript
export const specialStatusTags = [
  'Woman-Owned', 'Minority-Owned', 'Social Equity', 'Veteran-Owned',
  'LGBTQ+-Owned', 'Disability-Owned', 'Economic Empowerment', 'MTC Priority',
] as const;
```

From astro.config.mjs:
```javascript
site: "https://dispensaries.meredithmcgee.org"
// No trailingSlash set yet -- this plan adds it
```

Badge color scheme (3 known tags in current data):
- Economic Empowerment: purple
- Social Equity: blue
- MTC Priority: amber
Additional tags from schema (not yet in data): Woman-Owned (pink), Minority-Owned (teal), Veteran-Owned (green), LGBTQ+-Owned (rose), Disability-Owned (indigo)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Astro components for dispensary detail page</name>
  <files>src/components/OwnershipBadge.astro, src/components/SiblingLinks.astro, src/components/JsonLd.astro, src/components/DispensaryCard.astro</files>
  <action>
    **src/components/OwnershipBadge.astro:**
    - Props: `{ tag: string }`
    - Renders a colored pill/badge span. Color map:
      - Economic Empowerment: `bg-purple-100 text-purple-800 border-purple-200`
      - Social Equity: `bg-blue-100 text-blue-800 border-blue-200`
      - MTC Priority: `bg-amber-100 text-amber-800 border-amber-200`
      - Woman-Owned: `bg-pink-100 text-pink-800 border-pink-200`
      - Minority-Owned: `bg-teal-100 text-teal-800 border-teal-200`
      - Veteran-Owned: `bg-green-100 text-green-800 border-green-200`
      - LGBTQ+-Owned: `bg-rose-100 text-rose-800 border-rose-200`
      - Disability-Owned: `bg-indigo-100 text-indigo-800 border-indigo-200`
      - Default/unknown: `bg-gray-100 text-gray-800 border-gray-200`
    - Styling: `inline-flex items-center px-3 py-1 rounded-full text-sm font-medium border`

    **src/components/JsonLd.astro:**
    - Props: `{ jsonLd: object }` (receives pre-built JSON-LD from page)
    - Renders: `<script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />`
    - Note: Use `set:html` to avoid HTML escaping of JSON content (per research pitfall #3)

    **src/components/SiblingLinks.astro:**
    - Props: `{ siblings: Array<{ tradeName: string; slug: string; town: string | null }> }`
    - If siblings is empty, render nothing
    - Otherwise render a "Other Locations by This Owner" section with a list of links
    - Each link: `<a href="/dispensary/{slug}/">{tradeName}</a>` with town in parentheses if available
    - Style: subtle section with links in green-700, hover:underline

    **src/components/DispensaryCard.astro:**
    - Props: `{ dispensary: Dispensary, narrative: { text: string; type: string }, siblings: Array<...> }`
    - Layout: centered card (max-w-2xl mx-auto) on subtle background, with shadow and rounded corners. "Polished restaurant guide" feel.
    - Sections (top to bottom):
      1. **Header:** dispensary.tradeName as h1 (text-2xl/3xl font-bold). Immediately below, ownership badges row using OwnershipBadge component for each specialStatusTags entry.
      2. **Owner section** (hidden if researchInconclusive): Owner/parent company name. Show parentCompany if different from owner, otherwise just owner.
      3. **Narrative section** (hidden if researchInconclusive): render narrative.text as a paragraph. If type is "pending", style with italic and muted color. If type is "inconclusive", show "Ownership information for this dispensary is being researched." notice in a subtle info box.
      4. **Facts section:** Address as tappable Google Maps link (`https://www.google.com/maps/search/?api=1&query={encoded address, town, MA}`), phone as `tel:` link (strip non-digit/plus chars for href, display formatted number), license type as plain text. Use a structured list or grid with labels.
      5. **Sibling links:** SiblingLinks component at bottom.
    - For researchInconclusive records: show header (name + badges), facts section, and a notice box saying "Ownership information for this dispensary is being researched." Do NOT show owner, narrative, or sibling sections.
    - Include a back link at top: `<a href="/">Back to Directory</a>` (simple text link, left-aligned above the card)
  </action>
  <verify>
    <automated>npx astro check 2>&1 | head -20</automated>
  </verify>
  <done>4 Astro components created. OwnershipBadge renders colored pills. JsonLd injects script tag. SiblingLinks renders cross-links. DispensaryCard assembles the full profile card with 3-state conditional rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Create dynamic route, configure trailing slash, verify 525-page build</name>
  <files>src/pages/dispensary/[slug].astro, astro.config.mjs</files>
  <action>
    **astro.config.mjs:**
    Add `trailingSlash: 'always'` to the defineConfig object. This ensures all URLs end with `/` matching the `/dispensary/[slug]/` convention. Per research open question #1.

    **src/pages/dispensary/[slug].astro:**
    1. In frontmatter, import: getCollection from 'astro:content', BaseLayout, DispensaryCard, JsonLd, all lib functions (generateNarrative, buildJsonLd, groupByParentCompany, getSiblings, buildMetaTitle, buildMetaDescription).
    2. `getStaticPaths()`:
       - `const dispensaries = await getCollection('dispensaries');`
       - Build sibling map once: `const siblingMap = groupByParentCompany(dispensaries.map(e => e.data));`
       - Return dispensaries.map producing params (slug: entry.data.slug) and props (dispensary: entry.data, siblings from getSiblings).
       - IMPORTANT: Use `entry.data.slug` for routing params, NOT `entry.id` (which is licenseNumber).
    3. In page frontmatter (outside getStaticPaths):
       - Destructure `{ dispensary, siblings }` from Astro.props
       - Generate narrative: `const narrative = generateNarrative(dispensary);`
       - Build JSON-LD: `const jsonLd = buildJsonLd(dispensary, Astro.site?.toString().replace(/\/$/, '') ?? '');`
       - Build SEO meta using tested lib functions from Plan 01:
         - `const title = buildMetaTitle(dispensary.tradeName);` (produces "Who Owns [Name]? | MA Cannabis Directory" per DETL-07 locked decision)
         - `const description = buildMetaDescription(dispensary);`
    4. Template:
       ```
       <BaseLayout title={title} description={description}>
         <JsonLd jsonLd={jsonLd} />
         <main class="py-8 px-4">
           <DispensaryCard dispensary={dispensary} narrative={narrative} siblings={siblings} />
         </main>
       </BaseLayout>
       ```

    **Build and verify:**
    Run `npx astro build` and verify:
    - Build succeeds without errors
    - 525 HTML files generated in dist/dispensary/*/index.html
    - Spot-check 3 pages: one normal, one needsNarrative, one researchInconclusive
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -5 && ls dist/dispensary/ | wc -l</automated>
  </verify>
  <done>525 dispensary detail pages built. Each has unique URL at /dispensary/[slug]/. Pages display profile card with name, badges, owner, narrative, facts, sibling links. JSON-LD present. SEO meta title/description unique per page (generated by unit-tested lib functions). researchInconclusive pages correctly hide owner/narrative/siblings.</done>
</task>

</tasks>

<verification>
- `npx astro build` succeeds and produces 525 pages in dist/dispensary/
- Spot-check dist/dispensary/the-haven-center/index.html contains JSON-LD with @type Store
- Spot-check a researchInconclusive page hides owner section
- Spot-check meta title follows "Who Owns [Name]? | MA Cannabis Directory" format
- All existing tests still pass: `npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- 525 HTML pages generated at /dispensary/[slug]/ URLs
- Each page has: name, badges, owner info, narrative, address (maps link), phone (tel link), license type
- JSON-LD with schema.org Store on every page
- Unique meta title and description per page (via tested buildMetaTitle/buildMetaDescription)
- Sibling cross-links for multi-location owners
- researchInconclusive pages show only basic info with researching notice
- trailingSlash: 'always' configured
</success_criteria>

<output>
After completion, create `.planning/phases/03-detail-pages/03-02-SUMMARY.md`
</output>
