---
phase: 08-data-freshness-and-launch-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified: []
autonomous: false
requirements:
  - DSGN-02

must_haves:
  truths:
    - "All 525 detail pages are built and accessible"
    - "Homepage loads acceptably fast on simulated 3G"
    - "Sitemap includes all expected pages"
    - "Site is launch-ready with no blocking issues"
  artifacts: []
  key_links: []
---

<objective>
Verify performance meets DSGN-02 target (homepage under 2s on 3G), validate all 525 detail pages build correctly, and confirm sitemap completeness. Final launch readiness checkpoint.

Purpose: Confirm the site meets performance targets and all pages are accessible before launch. This is the last phase -- completing it means v1 is ready to share.
Output: Verified performance metrics and launch-ready confirmation.
</objective>

<execution_context>
@C:/Users/mmcge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mmcge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-freshness-and-launch-readiness/08-CONTEXT.md
@.planning/phases/08-data-freshness-and-launch-readiness/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate build output — page count, sitemap completeness, and performance check</name>
  <files></files>
  <action>
1. Run a full production build: `npm run build`

2. Verify page count:
   - Count all index.html files in dist/dispensary/ — expect 525
   - Count total index.html files in dist/ — expect 528+ (525 dispensary + homepage + terms + correct)

3. Verify sitemap completeness:
   - Read dist/sitemap-index.xml and follow references to actual sitemap files
   - Confirm the sitemap contains URLs for all dispensary pages
   - Confirm homepage, /terms/, and /correct/ are included
   - Confirm all URLs use trailing slashes (matching trailingSlash: "always" config)

4. Check client-side JS payload size:
   - List JS files in dist/ with sizes
   - Note the Fuse.js search index JSON size
   - If search index is over 100KB, check whether the script tag loading it uses `defer` or `async`

5. Performance assessment (best-effort, per user decision):
   - The site is text-only on Cloudflare CDN with no images
   - Report the total dist/ size and largest files
   - Note: actual Lighthouse 3G test requires browser DevTools (manual verification in next task)
   - If there are obvious performance issues (very large JS bundles, unminified output), note them

6. Run `npm test` to confirm no regressions.

Report findings as structured output for the checkpoint task.
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/MACannabisProject && npm run build 2>&1 | tail -5 && echo "---PAGE COUNT---" && find dist/dispensary -name "index.html" | wc -l && echo "---TOTAL PAGES---" && find dist -name "index.html" | wc -l && echo "---SITEMAP---" && test -f dist/sitemap-index.xml && echo "EXISTS" && echo "---TESTS---" && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>Build produces 525 dispensary pages, 528+ total pages, sitemap-index.xml exists with all pages. Tests pass. Performance findings documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Launch readiness verification — performance and final review</name>
  <action>
Present the build validation results from Task 1 to the user. Guide them through the Lighthouse performance check and sitemap verification on the production site. This is the final checkpoint before v1 launch.
  </action>
  <verify>User confirms launch readiness by typing "approved"</verify>
  <done>User has verified: (1) Lighthouse performance is acceptable on simulated 3G, (2) sitemap loads on production, (3) README data update workflow is clear, (4) site is launch-ready.</done>
  <what-built>Complete Phase 8 deliverables: sitemap generation, README with data update workflow, launch checklist, and build validation. The site is now launch-ready.</what-built>
  <how-to-verify>
    1. Open https://dispensaries.meredithmcgee.org/ in Chrome
    2. Open DevTools (F12) -> Lighthouse tab
    3. Select "Performance" category only
    4. Set throttling to "Simulated throttling" (includes simulated slow 3G)
    5. Click "Analyze page load" — confirm performance score is reasonable (target: homepage loads under 2s on 3G)
    6. Visit https://dispensaries.meredithmcgee.org/sitemap-index.xml — confirm it loads and contains sitemap references
    7. Spot-check 2-3 dispensary URLs from the sitemap — confirm they load correctly
    8. Review README.md in the repo — confirm "Updating Data" section is clear and accurate
    9. Review the pre-launch checklist in README.md — confirm it covers Formspree setup

    Note: This is the final phase. Approving this checkpoint means v1 is launch-ready.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm launch readiness, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. 525 dispensary detail pages built successfully
2. Sitemap includes all pages with correct URLs and priorities
3. Homepage Lighthouse performance is acceptable on simulated 3G
4. All existing tests pass with no regressions
5. User confirms launch readiness
</verification>

<success_criteria>
- All 525 detail page URLs built in dist/
- Sitemap-index.xml generated and accessible
- Homepage loads under 2 seconds on simulated 3G (best-effort — text-only site on CDN should be well under)
- User approves final launch readiness checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/08-data-freshness-and-launch-readiness/08-02-SUMMARY.md`
</output>
