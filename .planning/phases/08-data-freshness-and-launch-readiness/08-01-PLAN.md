---
phase: 08-data-freshness-and-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - package.json
  - README.md
autonomous: true
requirements:
  - DATA-04
  - DSGN-02

must_haves:
  truths:
    - "Sitemap is generated at build time including all 525+ dispensary pages"
    - "robots.txt sitemap reference resolves to actual generated sitemap"
    - "README documents the complete data update workflow from XLSX to deployed site"
    - "Launch checklist documents Formspree setup and final data review steps"
  artifacts:
    - path: "astro.config.mjs"
      provides: "@astrojs/sitemap integration with priority configuration"
      contains: "sitemap"
    - path: "README.md"
      provides: "Project documentation with data update workflow and launch checklist"
      contains: "Updating Data"
  key_links:
    - from: "astro.config.mjs"
      to: "dist/sitemap-index.xml"
      via: "@astrojs/sitemap integration generates sitemap at build time"
      pattern: "sitemap"
    - from: "public/robots.txt"
      to: "dist/sitemap-index.xml"
      via: "robots.txt Sitemap directive matches generated filename"
      pattern: "sitemap-index.xml"
---

<objective>
Install @astrojs/sitemap integration for SEO, create README.md with data update workflow documentation and launch checklist.

Purpose: Fulfill DATA-04 (documented monthly update workflow) and prepare SEO infrastructure (sitemap) for launch. The sitemap is already referenced in robots.txt but not generated.
Output: Working sitemap generation in build, comprehensive README.md with data update workflow and launch checklist.
</objective>

<execution_context>
@C:/Users/mmcge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mmcge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-freshness-and-launch-readiness/08-CONTEXT.md
@.planning/phases/08-data-freshness-and-launch-readiness/08-RESEARCH.md

<interfaces>
<!-- Existing astro.config.mjs — sitemap integration added here -->
From astro.config.mjs:
```typescript
import { defineConfig } from "astro/config";
import tailwindcss from "@tailwindcss/vite";

export default defineConfig({
  site: "https://dispensaries.meredithmcgee.org",
  trailingSlash: "always",
  vite: {
    plugins: [tailwindcss()],
  },
});
```

From public/robots.txt:
```
User-agent: *
Allow: /

Sitemap: https://dispensaries.meredithmcgee.org/sitemap-index.xml
```

From package.json scripts:
```json
{
  "build": "npm run build:data && npm run build:search && astro build",
  "build:data": "npx tsx scripts/build-data.ts",
  "build:search": "npx tsx scripts/build-search-index.ts"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @astrojs/sitemap and configure with priority values</name>
  <files>astro.config.mjs, package.json</files>
  <action>
1. Install @astrojs/sitemap: `npm install @astrojs/sitemap`

2. Update astro.config.mjs to add the sitemap integration with a serialize function for priority configuration:
   - Import `sitemap` from `@astrojs/sitemap`
   - Add `sitemap({...})` to a new `integrations` array in the config
   - serialize function sets priorities:
     - Homepage (`/`): priority 1.0, changefreq 'monthly'
     - Dispensary detail pages (URL contains `/dispensary/`): priority 0.8, changefreq 'monthly'
     - All other pages (terms, correct): priority 0.3, changefreq 'yearly'
   - Keep existing `site`, `trailingSlash`, and `vite` config unchanged

3. Verify the build produces a sitemap-index.xml in dist/.

Per research: the site URL and trailingSlash are already configured correctly. The robots.txt already references sitemap-index.xml which is the default output filename.
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/MACannabisProject && npm run build 2>&1 | tail -20 && test -f dist/sitemap-index.xml && echo "SITEMAP EXISTS" && cat dist/sitemap-index.xml</automated>
  </verify>
  <done>Build succeeds, dist/sitemap-index.xml exists and references sitemap file(s) containing 525+ dispensary URLs plus homepage, terms, and correct pages. Existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md with data update workflow and launch checklist</name>
  <files>README.md</files>
  <action>
Create README.md at project root with the following sections:

1. **Header** — "MA Cannabis Dispensary Ownership Directory" with a one-line description: a transparent, searchable directory answering "who owns this dispensary?" for every licensed cannabis dispensary in Massachusetts.

2. **Development** section:
   - Prerequisites: Node.js 20+
   - Install: `npm install`
   - Dev server: `npm run dev` (runs build-data then starts Astro dev server)
   - Production build: `npm run build` (runs build-data, build-search-index, then Astro build)
   - Preview: `npm run preview`
   - Tests: `npm test`

3. **Updating Data** section (fulfills DATA-04):
   Document the current workflow as-is:
   - Step 1: Update the source XLSX file (data/MACannabisDirectory.xlsx) with new or corrected records
   - Step 2: Run `npm run build:data` to regenerate src/data/dispensaries.json and src/data/stats.json
   - Step 3: Review the build output — check record count (should be 525 or more), look for validation errors
   - Step 4: Run `npm run build` to verify the full site builds successfully (pages generated, sitemap updated)
   - Step 5: Commit changes and push to main — Cloudflare Pages auto-deploys
   - Step 6: After deploy, spot-check a few dispensary pages on production to confirm changes
   - Include a "What to check after a data update" subsection: build success, record count matches expectations, no Zod validation errors, spot-check a few detail pages

4. **Pre-Launch Checklist** section:
   - [ ] Create a Formspree form and replace `FORMSPREE_ID_PLACEHOLDER` in `src/pages/correct.astro` with the real form ID
   - [ ] Review 525 records for obvious data errors before sharing publicly
   - [ ] Spot-check 5-10 dispensary detail pages for correct ownership info
   - [ ] Verify the correction form submits successfully on the deployed site

5. **Tech Stack** section (brief):
   - Astro 5 (static site generation)
   - Tailwind CSS v4
   - Fuse.js (client-side search)
   - Cloudflare Pages (hosting, auto-deploy from GitHub)

6. **License** — leave a placeholder or omit (user can add later).

Per user decision: Document the current flow as-is. No automation changes to the pipeline. Honest about manual steps.
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/MACannabisProject && test -f README.md && grep -c "Updating Data" README.md && grep -c "FORMSPREE_ID_PLACEHOLDER" README.md && grep -c "build:data" README.md && echo "README VALID"</automated>
  </verify>
  <done>README.md exists with clear "Updating Data" section documenting the XLSX -> build-data.ts -> review -> commit -> auto-deploy workflow. Pre-launch checklist includes Formspree setup and data review steps. DATA-04 requirement is satisfied.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes successfully (all 528+ pages + sitemap)
2. `dist/sitemap-index.xml` exists and references sitemap files
3. Sitemap contains URLs for all dispensary pages, homepage, terms, and correct pages
4. `npm test` passes (no regressions)
5. README.md contains "Updating Data" section with complete workflow
6. README.md contains pre-launch checklist with Formspree and data review items
</verification>

<success_criteria>
- Sitemap generated at build time with correct priorities (homepage 1.0, dispensary 0.8, others 0.3)
- robots.txt sitemap-index.xml reference resolves to actual generated file
- README documents the full data update lifecycle from XLSX edit to deployed site
- Launch checklist captures all manual pre-launch tasks
</success_criteria>

<output>
After completion, create `.planning/phases/08-data-freshness-and-launch-readiness/08-01-SUMMARY.md`
</output>
