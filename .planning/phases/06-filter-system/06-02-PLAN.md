---
phase: 06-filter-system
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: []
autonomous: false
requirements: [SRCH-04, SRCH-05, SRCH-07]
must_haves:
  truths:
    - "Filter pills visually toggle between active and inactive states"
    - "Card grid immediately updates when a filter is applied"
    - "Counts on pills and county options reflect current filter/search state"
    - "URL can be shared and filter state restores correctly"
    - "Back button undoes filter changes"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the filter system by the user.

Purpose: Confirm that tag pills, county dropdown, and filter-search composition work correctly in a real browser with real data.
Output: User approval of Phase 6 filter implementation.
</objective>

<execution_context>
@C:/Users/mmcge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mmcge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-filter-system/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for verification</name>
  <files></files>
  <action>
    Run `npx astro dev` to start the development server. Verify it starts without errors and report the local URL.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -3</automated>
  </verify>
  <done>Dev server running, build confirmed successful.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of filter system</name>
  <action>
    Present the verification checklist to the user and wait for approval.
  </action>
  <what-built>
    Phase 6 filter system: 3 ownership tag pills (MTC Priority, Economic Empowerment, Social Equity), county dropdown (14 counties), "Clear all" button, URL state persistence, dynamic counts, and search composition.
  </what-built>
  <how-to-verify>
    Visit http://localhost:4321 and test the following:

    **Tag pills (SRCH-04):**
    1. Verify 3 pills appear below the search bar area and above "Showing X of 525"
    2. Click "Social Equity" — grid should show ~67 dispensaries, pill turns blue
    3. Click "Social Equity" again — deactivates, grid returns to 525

    **Multiple pills with OR logic (SRCH-05):**
    4. Click "Social Equity" then "Economic Empowerment" — should show union (~105 dispensaries, since 0 records have both tags)
    5. Verify counts on inactive pills update to reflect how many would be added

    **County dropdown (SRCH-07):**
    6. Select "Suffolk" county — grid filters to Suffolk dispensaries only
    7. Verify pill counts update to show Suffolk-only counts

    **Filter + search composition:**
    8. Type "Boston" in search, then click "Social Equity" — only Social Equity dispensaries in Boston results
    9. Clear search — Social Equity filter should remain active

    **URL persistence:**
    10. Apply a tag filter + county filter
    11. Copy the URL (should contain ?tags= and ?county= params)
    12. Open in new tab — filters should be restored

    **Back button:**
    13. Apply several filters one at a time
    14. Press browser back button — each press should undo the last filter change

    **Clear all:**
    15. With filters active, click "Clear all" — everything resets
    16. "Clear all" should disappear when no filters are active

    **Mobile:**
    17. Resize browser to mobile width — pills should wrap, dropdown should fit
  </how-to-verify>
  <verify>User confirms all behaviors work correctly</verify>
  <done>User types "approved" confirming filter system works as specified.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
User visually confirms all filter behaviors work correctly in the browser.
</verification>

<success_criteria>
- User approves filter system functionality and appearance
- All filter interactions work as specified
- Filters compose correctly with search
- URL state persistence works
- Mobile layout is acceptable
</success_criteria>

<output>
After completion, create `.planning/phases/06-filter-system/06-02-SUMMARY.md`
</output>
